# Environment Variables for Serverless Contact Form

# Required: SendGrid Configuration
# Get your API key from https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Required: Verified sender email address in SendGrid
# This must be a verified sender or domain in your SendGrid account
SENDGRID_FROM=noreply@yourdomain.com

# Required: Recipient email for contact form submissions
CONTACT_TO=contact@yourdomain.com

# Optional: Google reCAPTCHA v3 Secret Key
# Get your keys from https://www.google.com/recaptcha/admin
# If not set, reCAPTCHA verification will be skipped
RECAPTCHA_SECRET=your_recaptcha_secret_key_here

# Optional: Rate Limiting Configuration
# Maximum number of requests allowed per time window
RATE_LIMIT_MAX=5

# Time window in minutes for rate limiting
RATE_LIMIT_WINDOW_MIN=15

# Optional: Upstash Redis for Persistent Rate Limiting
# Uncomment and configure these if using Upstash Redis instead of in-memory rate limiting
# Get your credentials from https://console.upstash.com/
# UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here
