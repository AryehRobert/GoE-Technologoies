# SendGrid Configuration (Required)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Verified sender email address in SendGrid
SENDGRID_FROM=noreply@yourdomain.com

# Email address to receive contact form submissions
CONTACT_TO=contact@yourdomain.com

# reCAPTCHA v3 Configuration (Optional but recommended)
# Get your keys from: https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET=your_recaptcha_secret_key_here
# Note: You'll also need to add the site key to contact.html

# Rate Limiting Configuration (Optional)
# Maximum number of requests allowed per IP within the time window
RATE_LIMIT_MAX=5

# Time window in minutes for rate limiting
RATE_LIMIT_WINDOW_MIN=60

# Upstash Redis Configuration (Optional - for production rate limiting)
# Uncomment and configure these if using Upstash Redis for distributed rate limiting
# Get your credentials from: https://console.upstash.com/
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here
